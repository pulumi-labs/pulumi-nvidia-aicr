{
  "name": "nvidia-aicr",
  "displayName": "NVIDIA AI Cluster Runtime",
  "description": "Deploy validated NVIDIA AI Cluster Runtime (AICR) recipes for GPU-accelerated Kubernetes clusters.",
  "keywords": [
    "pulumi",
    "nvidia",
    "aicr",
    "gpu",
    "kubernetes",
    "category/cloud"
  ],
  "homepage": "https://github.com/pulumi-labs/pulumi-nvidia-aicr",
  "license": "Apache-2.0",
  "repository": "https://github.com/pulumi-labs/pulumi-nvidia-aicr",
  "logoUrl": "https://raw.githubusercontent.com/pulumi-labs/pulumi-nvidia-aicr/main/sdk/dotnet/logo.png",
  "pluginDownloadURL": "github://api.github.com/pulumi-labs/pulumi-nvidia-aicr",
  "publisher": "Pulumi Labs",
  "language": {
    "csharp": {
      "packageReferences": {
        "Pulumi": "3.*"
      },
      "respectSchemaVersion": true,
      "rootNamespace": "Pulumi.Labs"
    },
    "go": {
      "generateResourceContainerTypes": true,
      "importBasePath": "github.com/pulumi-labs/pulumi-nvidia-aicr/sdk/go/nvidiaaicr",
      "respectSchemaVersion": true
    },
    "java": {
      "basePackage": "com.pulumi.labs",
      "dependencies": {
        "com.pulumi:pulumi": "1.0.0"
      },
      "respectSchemaVersion": true
    },
    "nodejs": {
      "dependencies": {
        "@pulumi/pulumi": "^3.142.0"
      },
      "devDependencies": {
        "@types/node": "^18",
        "typescript": "^4.3.5"
      },
      "packageDescription": "Deploy validated NVIDIA AI Cluster Runtime (AICR) recipes for GPU-accelerated Kubernetes clusters.",
      "packageName": "@pulumi-labs/nvidia-aicr",
      "respectSchemaVersion": true
    },
    "python": {
      "packageName": "pulumi_labs_nvidia_aicr",
      "pyproject": {
        "enabled": true
      },
      "requires": {
        "pulumi": ">=3.165.0,<4.0.0"
      },
      "respectSchemaVersion": true
    }
  },
  "config": {},
  "types": {
    "nvidia-aicr:index:CheckResult": {
      "description": "The outcome of one validator check.",
      "properties": {
        "message": {
          "type": "string",
          "description": "The check's failure or diagnostic message, if any."
        },
        "name": {
          "type": "string",
          "description": "The check's name (e.g. \"gpu-operator-health\")."
        },
        "phase": {
          "type": "string",
          "description": "The validation phase the check ran in."
        },
        "status": {
          "type": "string",
          "description": "The check's status: \"passed\", \"failed\", \"skipped\", \"pending\", or \"other\"."
        }
      },
      "type": "object",
      "required": [
        "name",
        "phase",
        "status",
        "message"
      ]
    },
    "nvidia-aicr:index:ComponentOverride": {
      "description": "Per-component override settings. Each field is optional; only the fields\nyou set are applied on top of the recipe defaults.",
      "properties": {
        "namespace": {
          "type": "string",
          "description": "Override the target Kubernetes namespace."
        },
        "values": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Additional or override Helm values, deep-merged on top of the\nrecipe-resolved values.\n\nMerge semantics: nested maps merge recursively; scalars and arrays replace\nthe recipe's value; setting a key to null removes it from the\nrecipe-resolved values, restoring the chart's own default for that key.\nNote the null asymmetry: a null *in the recipe data* is passed through to\nHelm (explicitly clearing the chart default), while a null *here* removes\nthe recipe's setting. There is currently no way to pass a literal null\nthrough to Helm from this input — and some language SDKs drop null map\nentries during serialization before they reach the provider at all."
        },
        "version": {
          "type": "string",
          "description": "Override the Helm chart version. If unset, the recipe-pinned version is used."
        }
      },
      "type": "object"
    },
    "nvidia-aicr:index:RecipeCriteria": {
      "description": "Recipe-selection criteria, mirroring ClusterStack's accelerator / service /\nintent / os / platform / nodes inputs, plus the skipComponents the stack\ndeployed without. Wire a ClusterStack's `criteria` output here so deployment\nand validation resolve the identical recipe and agree on which of its\ncomponents are in scope.",
      "properties": {
        "accelerator": {
          "type": "string",
          "description": "GPU accelerator type. Supported values: \"h100\", \"gb200\", \"b200\", \"rtx-pro-6000\" (eks/lke only)."
        },
        "intent": {
          "type": "string",
          "description": "Workload intent. Supported values: \"training\", \"inference\"."
        },
        "nodes": {
          "type": "integer",
          "description": "Worker-node count hint used to size the recipe."
        },
        "os": {
          "type": "string",
          "description": "Operating system flavor of the worker nodes. Leave unset for OS-agnostic\nresolution. Supported values: \"ubuntu\", \"cos\", \"ol\"."
        },
        "platform": {
          "type": "string",
          "description": "ML platform/framework. Supported values: \"kubeflow\" (training),\n\"dynamo\" (inference), \"nim\" (inference, eks with h100 or rtx-pro-6000 only).\nkubeflow and dynamo have no recipes on lke/bcm."
        },
        "service": {
          "type": "string",
          "description": "Kubernetes service. Supported values: \"aks\", \"bcm\", \"eks\", \"gke\", \"kind\", \"lke\", \"oke\"."
        },
        "skipComponents": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Recipe components the stack intentionally did not deploy (ClusterStack's\n`skipComponents`). ValidationRun treats them as out of scope rather than\nmissing: checks that presuppose one of them (e.g. the gpu-operator health,\nDCGM metrics, and GPU-HPA checks when \"gpu-operator\" is skipped) are reported\n\"skipped\" with a reason instead of failing, and the SDK's component-aware\nchecks see the components as disabled. A ClusterStack's `criteria` output\ncarries its own skipComponents, so wiring it keeps validation aligned with\nthe deployed subset automatically. Skipping does not verify a replacement\nyou run yourself — those checks are simply not made."
        }
      },
      "type": "object",
      "required": [
        "accelerator",
        "service",
        "intent"
      ]
    },
    "nvidia-aicr:index:Toleration": {
      "description": "A Kubernetes pod toleration applied to validation workload pods.",
      "properties": {
        "effect": {
          "type": "string",
          "description": "The taint effect to match: \"NoSchedule\", \"PreferNoSchedule\", or\n\"NoExecute\". Empty matches all effects."
        },
        "key": {
          "type": "string",
          "description": "The taint key the toleration applies to. Empty means match all keys\n(with operator \"Exists\")."
        },
        "operator": {
          "type": "string",
          "description": "Key-value relationship: \"Exists\" or \"Equal\". Default: \"Equal\"."
        },
        "tolerationSeconds": {
          "type": "integer",
          "description": "How long the pod tolerates a \"NoExecute\" taint, in seconds."
        },
        "value": {
          "type": "string",
          "description": "The taint value to match (with operator \"Equal\")."
        }
      },
      "type": "object"
    }
  },
  "provider": {},
  "resources": {
    "nvidia-aicr:index:ClusterStack": {
      "properties": {
        "componentCount": {
          "type": "integer",
          "description": "Number of components deployed."
        },
        "criteria": {
          "$ref": "#/types/nvidia-aicr:index:RecipeCriteria",
          "description": "The canonicalized recipe criteria this stack resolved with, including its\n`skipComponents`. Wire it into a ValidationRun's `criteria` input so deployment and\nvalidation share a single source of truth — the same recipe, and the same\ncomponents in scope."
        },
        "deployedComponents": {
          "type": "array",
          "items": {
            "type": "string",
            "plain": true
          },
          "description": "Names of all components deployed as part of this stack, in topological order."
        },
        "recipeName": {
          "type": "string",
          "description": "The resolved AICR recipe name (e.g., \"h100-eks-ubuntu-training-kubeflow\")."
        },
        "recipeVersion": {
          "type": "string",
          "description": "The AICR recipe data version embedded in this provider build."
        }
      },
      "required": [
        "recipeName",
        "recipeVersion",
        "deployedComponents",
        "componentCount",
        "criteria"
      ],
      "inputProperties": {
        "accelerator": {
          "type": "string",
          "plain": true,
          "description": "GPU accelerator type. Selects the AICR recipe family.\n\nSupported values: \"h100\", \"gb200\", \"b200\", \"rtx-pro-6000\" (eks/lke only —\nthe services with rtx-pro-6000-tuned recipes in the pinned AICR data)."
        },
        "componentOverrides": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/types/nvidia-aicr:index:ComponentOverride"
          },
          "description": "Per-component overrides. Map of AICR component name to override settings\n(version, namespace, Helm values). Values are deep-merged with the recipe\ndefaults; only the keys you specify are changed."
        },
        "context": {
          "type": "string",
          "plain": true,
          "description": "Kubeconfig context to select. Defaults to the current-context in the kubeconfig."
        },
        "intent": {
          "type": "string",
          "plain": true,
          "description": "Workload intent. Selects between training-oriented and inference-oriented\ncomponent sets.\n\nSupported values: \"training\", \"inference\"."
        },
        "kubeconfig": {
          "type": "string",
          "description": "Kubeconfig contents (or path to a kubeconfig file) for the target cluster.\nAccepts computed outputs from cluster resources (e.g., an EKS cluster's\nKubeconfigJson). Mutually exclusive with `kubeconfigPath`.\n\nIf neither `kubeconfig` nor `kubeconfigPath` is set, the ambient kubeconfig\n(KUBECONFIG env var or ~/.kube/config) is used."
        },
        "kubeconfigPath": {
          "type": "string",
          "plain": true,
          "description": "Path to a kubeconfig file on disk. Mutually exclusive with `kubeconfig`.\nPrefer `kubeconfig` when chaining off a cluster resource's output."
        },
        "nodes": {
          "type": "integer",
          "plain": true,
          "description": "Worker-node count hint used to size the recipe (number of nodes, not GPUs).\nLeave unset to let AICR pick the default-sized recipe."
        },
        "os": {
          "type": "string",
          "plain": true,
          "description": "Operating system flavor of the worker nodes.\n\nSupported values: \"ubuntu\", \"cos\" (Container-Optimized OS, GKE only), \"ol\"\n(Oracle Linux, OKE) — the values with backing recipes in this provider's\npinned AICR data. Additional OS values (rhel, amazonlinux, talos) arrive\nthrough AICR SDK upgrades.\n\nLeave unset for OS-agnostic resolution: OS-pinned recipe overlays (kernel\ntuning, driver constraints) are skipped and the OS-agnostic recipe is used.\nSet it when the cluster's OS is known. Some combinations require an OS\n(e.g. gke requires \"cos\"; eks platform recipes require \"ubuntu\") and fail\nwith a message listing the valid values; kind recipes require it unset."
        },
        "platform": {
          "type": "string",
          "plain": true,
          "description": "ML platform/framework to layer on top of the base recipe.\n\nSupported values: \"kubeflow\" (training), \"dynamo\" (inference), \"nim\"\n(inference, eks with h100 or rtx-pro-6000 only). kubeflow and dynamo have no\nrecipes on lke/bcm in the pinned AICR data.\n\nLeave unset for the base recipe without a platform-specific runtime. Note\nthat intent=\"inference\" always includes an inference gateway (part of the\nbase inference stack); choosing a platform layers a runtime (\"dynamo\",\n\"nim\") on top. intent=\"training\" leaves training-runtime components out\nentirely when platform is unset."
        },
        "service": {
          "type": "string",
          "plain": true,
          "description": "Kubernetes service. Selects cloud-specific operators and storage drivers.\n\nSupported values: \"aks\", \"bcm\", \"eks\", \"gke\", \"kind\", \"lke\", \"oke\". bcm and\nlke are the cloud-neutral leaves (no hyperscaler CSI/EFA components) and\ndouble as stand-ins for providers without an AICR criteria value yet (e.g.\nCoreWeave CKS deploys the lke leaf). Use \"kind\" for local\nhardware-free development of the deployment pipeline."
        },
        "skipAwait": {
          "type": "boolean",
          "plain": true,
          "description": "If true, do not wait for each Helm release to become ready before continuing.\nFaster previews/updates at the cost of losing readiness signal. Default: false.",
          "default": false
        },
        "skipComponents": {
          "type": "array",
          "items": {
            "type": "string",
            "plain": true
          },
          "description": "Component names to exclude from the deployment. Useful for swapping in your\nown installation of a component (e.g., bring-your-own cert-manager) or for\ndeploying onto bare-metal where cloud-specific operators are not relevant."
        }
      },
      "requiredInputs": [
        "accelerator",
        "service",
        "intent"
      ],
      "isComponent": true
    },
    "nvidia-aicr:index:ValidationRun": {
      "description": "Runs an AICR recipe's empirical validation (cluster snapshot +\ndeployment/conformance/performance checks) against a live cluster and\nrecords structured per-check results in Pulumi state.\n\nThe run happens at deploy time (never at preview). The validation harness\ndeploys short-lived agent and validator Jobs plus per-run RBAC on the target\ncluster and cleans them up afterward; the validation namespace (default\n\"aicr-validation\") is created on first run and deliberately persists.\n\nPrivilege level: the snapshot-agent Job runs as a privileged container\n(bound to a read-only node-inspection ClusterRole), and validator Jobs run\nunder a per-run cluster-admin ClusterRoleBinding — both removed after the\nrun, including on cancellation. The kubeconfig identity needs RBAC to\ncreate/patch Namespaces, ClusterRoles, and ClusterRoleBindings.\n\nAny input change replaces the resource, which re-runs validation (delete is\na no-op). With `strict: true`, a failed run fails the update while persisting\nfull results, and validation re-runs on every subsequent `pulumi up` until it\npasses — use `dependsOn` to gate downstream resources on it.",
      "properties": {
        "completedAt": {
          "type": "string",
          "description": "When the run completed, RFC 3339."
        },
        "context": {
          "type": "string",
          "description": "Kubeconfig context to select. Defaults to the kubeconfig's current-context."
        },
        "criteria": {
          "$ref": "#/types/nvidia-aicr:index:RecipeCriteria",
          "description": "Recipe criteria. Wire a ClusterStack's `criteria` output here, or build\nthe object inline."
        },
        "ctrfReport": {
          "type": "string",
          "description": "The merged CTRF JSON report. Set only when `includeCtrfReport` is true."
        },
        "failed": {
          "type": "integer",
          "description": "Number of checks that failed."
        },
        "imagePullSecrets": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names of image pull Secrets (in the validation namespace) for the\nvalidation pods."
        },
        "imageRegistry": {
          "type": "string",
          "description": "Registry override for the snapshot-agent and validator images (air-gapped\nmirrors). Unset keeps the default registry (ghcr.io)."
        },
        "includeCtrfReport": {
          "type": "boolean",
          "description": "If true, the merged CTRF JSON report is stored in the `ctrfReport` output.\nDefault: false (state-size control; per-check results are always in\n`phaseResults`).",
          "default": false
        },
        "kubeconfig": {
          "type": "string",
          "description": "Kubeconfig contents for the target cluster. Mutually exclusive with\n`kubeconfigPath`. If neither is set, the ambient kubeconfig (KUBECONFIG env\nvar or ~/.kube/config) is used. Written to a mode-0600 temp file for the\nduration of the run and removed afterward.",
          "secret": true
        },
        "kubeconfigPath": {
          "type": "string",
          "description": "Path to a kubeconfig file on disk. Mutually exclusive with `kubeconfig`."
        },
        "namespace": {
          "type": "string",
          "description": "Namespace for the validation harness — use a dedicated one: it hosts the\nprivileged agent and cluster-admin-bound validator Jobs. The SDK creates it\non first run and deliberately never deletes it. Must be a valid DNS-1123\nlabel and not a \"kube-\" system namespace. Default: \"aicr-validation\".",
          "default": "aicr-validation"
        },
        "nodeSelector": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Node selector applied to the validation workload pods. Use when GPU nodes\ncarry non-standard labels."
        },
        "other": {
          "type": "integer",
          "description": "Number of checks with an inconclusive outcome (crash, OOM, timeout;\nincludes CTRF \"pending\")."
        },
        "passed": {
          "type": "integer",
          "description": "Number of checks that passed."
        },
        "phaseResults": {
          "type": "array",
          "items": {
            "$ref": "#/types/nvidia-aicr:index:CheckResult"
          },
          "description": "Per-check results across all phases run."
        },
        "phases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Validation phases to run: any of \"deployment\", \"conformance\",\n\"performance\". Default: [\"deployment\", \"conformance\"] — \"performance\" is an\nexplicit opt-in (long-running and hardware-bound)."
        },
        "readinessMessage": {
          "type": "string",
          "description": "The readiness pre-flight failure message; empty unless status is\n\"readiness-failed\"."
        },
        "recipeDataVersion": {
          "type": "string",
          "description": "Recipe-data version assertion (not a pin). When set, it must equal this\nprovider build's embedded AICR recipe-data version or Create fails before\nany cluster work — wire `recipeDataVersion: stack.recipeVersion` to catch a\nprovider upgrade between deploy and validate."
        },
        "recipeName": {
          "type": "string",
          "description": "The resolved AICR recipe name that was validated."
        },
        "recipeVersion": {
          "type": "string",
          "description": "The AICR recipe data version that was validated."
        },
        "requireGpu": {
          "type": "boolean",
          "description": "Whether the snapshot agent requires GPU nodes. Set false for\nhardware-free clusters (kind). Default: true.",
          "default": true
        },
        "runId": {
          "type": "string",
          "description": "The run identifier labeling this run's cluster-side artifacts."
        },
        "skipped": {
          "type": "integer",
          "description": "Number of checks that were skipped."
        },
        "status": {
          "type": "string",
          "description": "Rollup verdict: \"passed\", \"failed\", or \"readiness-failed\". Checks with\nstatus \"other\" never flip the rollup (they are counted and listed).\nInfrastructure errors have no status — they fail the update outright."
        },
        "strict": {
          "type": "boolean",
          "description": "If true, a failed (or readiness-failed) validation fails the update while\nstill persisting the full results in state. The engine then re-runs\nvalidation on every subsequent `pulumi up` until it passes. Default: false —\nresults are recorded and the update succeeds; read the `status` output.",
          "default": false
        },
        "timeoutMinutes": {
          "type": "integer",
          "description": "Overall run timeout in minutes. Default: 30. Maximum: 1440 (24 hours).",
          "default": 30
        },
        "tolerations": {
          "type": "array",
          "items": {
            "$ref": "#/types/nvidia-aicr:index:Toleration"
          },
          "description": "Tolerations applied to the validation workload pods (e.g. NCCL benchmark\npods). Required in practice on tainted GPU node groups; when unset, the\nvalidator's default tolerate-all is kept."
        },
        "triggers": {
          "type": "array",
          "items": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Arbitrary values; changing any of them replaces the resource and re-runs\nvalidation (every input change does). Wire `triggers: [stack.deployedComponents]`\nfor re-validation cadence."
        }
      },
      "required": [
        "criteria",
        "status",
        "readinessMessage",
        "phaseResults",
        "passed",
        "failed",
        "skipped",
        "other",
        "runId",
        "completedAt",
        "recipeName",
        "recipeVersion"
      ],
      "inputProperties": {
        "context": {
          "type": "string",
          "description": "Kubeconfig context to select. Defaults to the kubeconfig's current-context."
        },
        "criteria": {
          "$ref": "#/types/nvidia-aicr:index:RecipeCriteria",
          "description": "Recipe criteria. Wire a ClusterStack's `criteria` output here, or build\nthe object inline."
        },
        "imagePullSecrets": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names of image pull Secrets (in the validation namespace) for the\nvalidation pods."
        },
        "imageRegistry": {
          "type": "string",
          "description": "Registry override for the snapshot-agent and validator images (air-gapped\nmirrors). Unset keeps the default registry (ghcr.io)."
        },
        "includeCtrfReport": {
          "type": "boolean",
          "description": "If true, the merged CTRF JSON report is stored in the `ctrfReport` output.\nDefault: false (state-size control; per-check results are always in\n`phaseResults`).",
          "default": false
        },
        "kubeconfig": {
          "type": "string",
          "description": "Kubeconfig contents for the target cluster. Mutually exclusive with\n`kubeconfigPath`. If neither is set, the ambient kubeconfig (KUBECONFIG env\nvar or ~/.kube/config) is used. Written to a mode-0600 temp file for the\nduration of the run and removed afterward.",
          "secret": true
        },
        "kubeconfigPath": {
          "type": "string",
          "description": "Path to a kubeconfig file on disk. Mutually exclusive with `kubeconfig`."
        },
        "namespace": {
          "type": "string",
          "description": "Namespace for the validation harness — use a dedicated one: it hosts the\nprivileged agent and cluster-admin-bound validator Jobs. The SDK creates it\non first run and deliberately never deletes it. Must be a valid DNS-1123\nlabel and not a \"kube-\" system namespace. Default: \"aicr-validation\".",
          "default": "aicr-validation"
        },
        "nodeSelector": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Node selector applied to the validation workload pods. Use when GPU nodes\ncarry non-standard labels."
        },
        "phases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Validation phases to run: any of \"deployment\", \"conformance\",\n\"performance\". Default: [\"deployment\", \"conformance\"] — \"performance\" is an\nexplicit opt-in (long-running and hardware-bound)."
        },
        "recipeDataVersion": {
          "type": "string",
          "description": "Recipe-data version assertion (not a pin). When set, it must equal this\nprovider build's embedded AICR recipe-data version or Create fails before\nany cluster work — wire `recipeDataVersion: stack.recipeVersion` to catch a\nprovider upgrade between deploy and validate."
        },
        "requireGpu": {
          "type": "boolean",
          "description": "Whether the snapshot agent requires GPU nodes. Set false for\nhardware-free clusters (kind). Default: true.",
          "default": true
        },
        "strict": {
          "type": "boolean",
          "description": "If true, a failed (or readiness-failed) validation fails the update while\nstill persisting the full results in state. The engine then re-runs\nvalidation on every subsequent `pulumi up` until it passes. Default: false —\nresults are recorded and the update succeeds; read the `status` output.",
          "default": false
        },
        "timeoutMinutes": {
          "type": "integer",
          "description": "Overall run timeout in minutes. Default: 30. Maximum: 1440 (24 hours).",
          "default": 30
        },
        "tolerations": {
          "type": "array",
          "items": {
            "$ref": "#/types/nvidia-aicr:index:Toleration"
          },
          "description": "Tolerations applied to the validation workload pods (e.g. NCCL benchmark\npods). Required in practice on tainted GPU node groups; when unset, the\nvalidator's default tolerate-all is kept."
        },
        "triggers": {
          "type": "array",
          "items": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Arbitrary values; changing any of them replaces the resource and re-runs\nvalidation (every input change does). Wire `triggers: [stack.deployedComponents]`\nfor re-validation cadence."
        }
      },
      "requiredInputs": [
        "criteria"
      ]
    }
  }
}
